import format from 'chalk';

export default format;

export const header = format.magenta;
export const section = format.bold;
export const desc = format.reset;
export const name = format.cyan;
export const info = format.yellow;
export const secondary = format.dim;
